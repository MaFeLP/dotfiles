post_install() {
    cat <<EOF

Notice:

To enable this customization, run the following commands:

mkdir -p ~/.config/zsh ~/.local/state/zsh ~/.config/environment.d/
touch ~/.config/zsh/.zshrc
ln -s /etc/zsh/systemd_environment.env ~/.config/environment.d/default_env.conf
EOF
}
